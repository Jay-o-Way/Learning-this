<Window
    x:Class="Pass_it_on.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pass_it_on"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >
<!--
    Width="300"
    Height="400"
    ExtendsContentIntoTitleBar="True"
    Title="Pass it on"
-->

    <StackPanel Width="400">
        <ComboBox Header="Subject" x:Uid="CB_text" x:Name="SortingHat" IsEditable="True" HorizontalAlignment="Stretch"
            >
            <!--
            ItemsSource="{x:Bind Subjects}"
            TextSubmitted="AtTextSubmitted"
            -->
            <ComboBoxItem Content="Alfa"/>
            <ComboBoxItem Content="Bravo"/>
            <ComboBoxItem Content="Charly"/>
        </ComboBox>
        <StackPanel Orientation="Horizontal">
            <Button Name="CreateButton" Content="Create" Width="100" IsEnabled="{Binding}" Click="CreateNewSubject"/>
            <Button Name="EditButton" Content="Edit" Width="100" IsEnabled="{Binding}" Click="EditSubject"/>
            <Button Name="DeleteButton" Content="Delete" Width="100" IsEnabled="{Binding}" Click="DeleteSubject"/>
        </StackPanel>
        <TextBox x:Uid="SubjectTitle" PlaceholderText="Subject title" IsEnabled="False"/>
        <TextBox x:Uid="SubjectLocation" PlaceholderText="Subject location" IsEnabled="False"/>
        <TextBox x:Uid="SubjectLogin" PlaceholderText="Subject log-in" IsEnabled="False"/>
        <TextBox x:Uid="SubjectPassword" PlaceholderText="Subject password" IsEnabled="False"/>
        <Button Name="SaveButton" Visibility="Collapsed" Content="Save" Click="SaveEdit"/>
        <Button Name="CancelButton" Visibility="Collapsed" Content="Cancel" Click="CancelEdit"/>
    </StackPanel>
</Window>